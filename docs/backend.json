
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile within the FamilyTree application, primarily for authentication and ownership tracking of family trees. This entity does not store sensitive authentication credentials like passwords, relying on an external authentication system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity. Matches Firebase Auth UID."
        },
        "username": {
          "type": "string",
          "description": "A unique display name for the user."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "username",
        "createdAt"
      ]
    },
    "UserEmail": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserEmail",
      "type": "object",
      "description": "A mapping from a user's email to their UID for sharing purposes.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID (from Firebase Auth)."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the record was created.",
          "format": "date-time"
        }
      },
      "required": [
        "uid",
        "email",
        "createdAt"
      ]
    },
    "FamilyTree": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FamilyTree",
      "type": "object",
      "description": "Represents a single family tree project owned by a user, containing multiple people and relationships.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the FamilyTree entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile who owns this family tree. (Relationship: UserProfile 1:N FamilyTree)"
        },
        "treeName": {
          "type": "string",
          "description": "The user-defined name for the family tree."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the family tree was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the family tree was last updated.",
          "format": "date-time"
        },
        "language": {
          "type": "string",
          "description": "The selected language for the tree.",
          "enum": [
            "he",
            "en"
          ]
        },
        "privacy": {
          "type": "string",
          "description": "The privacy setting for the tree.",
          "enum": [
            "private",
            "link",
            "public"
          ]
        },
        "shareToken": {
          "type": "string",
          "description": "A unique token for sharing the tree via a link."
        },
        "personCount": {
          "type": "number",
          "description": "Denormalized count of people in the tree."
        },
        "relationshipCount": {
          "type": "number",
          "description": "Denormalized count of relationships in the tree."
        },
        "coverPhotoURL": {
          "type": "string",
          "description": "A URL to the tree's cover photo. Must be a valid HTTPS URL.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "userId",
        "treeName",
        "createdAt",
        "updatedAt"
      ]
    },
    "Person": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Person",
      "type": "object",
      "description": "Represents an individual person within a family tree, serving as a node in the interactive canvas.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Person entity."
        },
        "treeId": {
          "type": "string",
          "description": "Reference to the FamilyTree this person belongs to. (Relationship: FamilyTree 1:N Person)"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the person."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the person."
        },
        "middleName": {
          "type": "string",
          "description": "The middle name of the person."
        },
        "previousFirstName": {
          "type": "string",
          "description": "A previous first name of the person."
        },
        "maidenName": {
          "type": "string",
          "description": "The person's last name before marriage (maiden name)."
        },
        "nickname": {
          "type": "string",
          "description": "A nickname for the person."
        },
        "gender": {
          "type": "string",
          "description": "The gender of the person (male, female, or other)."
        },
        "birthDate": {
          "type": "string",
          "description": "The person's birth date in YYYY-MM-DD format."
        },
        "deathDate": {
          "type": "string",
          "description": "The person's death date in YYYY-MM-DD format. Must be after birthDate if both are provided."
        },
        "birthPlace": {
          "type": "string",
          "description": "The place where the person was born."
        },
        "aliyahDate": {
          "type": "string",
          "description": "The date the person immigrated to Israel (Aliyah date) in YYYY-MM-DD format."
        },
        "status": {
          "type": "string",
          "description": "The current status of the person (alive, deceased, or unknown)."
        },
        "religion": {
          "type": "string",
          "description": "The person's religion.",
          "enum": [
            "jewish",
            "christian",
            "muslim",
            "buddhist",
            "other"
          ]
        },
        "countryOfResidence": {
          "type": "string",
          "description": "The person's country of residence."
        },
        "cityOfResidence": {
          "type": "string",
          "description": "The person's city of residence."
        },
        "photoURL": {
          "type": "string",
          "description": "A URL to the person's profile photo. Must be a valid HTTPS URL.",
          "format": "uri"
        },
        "description": {
          "type": "string",
          "description": "A biographical description of the person, limited to 2000 characters."
        },
        "profession": {
          "type": "string",
          "description": "The person's profession or job."
        },
        "hobby": {
          "type": "string",
          "description": "The person's hobby."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when this person record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when this person record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "treeId",
        "firstName",
        "lastName",
        "gender",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "Relationship": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Relationship",
      "type": "object",
      "description": "Defines a typed connection between two people within a family tree.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Relationship entity."
        },
        "treeId": {
          "type": "string",
          "description": "Reference to the FamilyTree this relationship belongs to. (Relationship: FamilyTree 1:N Relationship)"
        },
        "personAId": {
          "type": "string",
          "description": "Reference to the first person in the relationship (e.g., the parent in a 'parent' relationship). (Relationship: Person 1:N Relationship)"
        },
        "personBId": {
          "type": "string",
          "description": "Reference to the second person in the relationship (e.g., the child in a 'parent' relationship). (Relationship: Person 1:N Relationship)"
        },
        "relationshipType": {
          "type": "string",
          "description": "The type of relationship (parent, spouse, adoptive_parent, step_parent).",
          "enum": [
            "parent",
            "spouse",
            "adoptive_parent",
            "step_parent",
            "sibling",
            "twin",
            "ex_spouse",
            "guardian",
            "separated",
            "partner",
            "ex_partner",
            "step_sibling",
            "widowed"
          ]
        },
        "startDate": {
          "type": "string",
          "description": "The start date of the relationship (e.g., marriage date) in YYYY-MM-DD format."
        },
        "endDate": {
          "type": "string",
          "description": "The end date of the relationship (e.g., divorce date) in YYYY-MM-DD format."
        },
        "notes": {
          "type": "string",
          "description": "Optional free-text notes about the relationship."
        },
        "manuallyEdited": {
          "type": "boolean",
          "description": "True if the relationship has been manually edited by the user, to prevent automatic overwrites."
        }
      },
      "required": [
        "id",
        "treeId",
        "personAId",
        "personBId",
        "relationshipType"
      ]
    },
    "SocialLink": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SocialLink",
      "type": "object",
      "description": "Stores external social media or website links associated with a specific person.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SocialLink entity."
        },
        "personId": {
          "type": "string",
          "description": "Reference to the Person this social link belongs to. (Relationship: Person 1:N SocialLink)"
        },
        "platform": {
          "type": "string",
          "description": "The platform of the social link (e.g., facebook, instagram, twitter, linkedin, website, other)."
        },
        "url": {
          "type": "string",
          "description": "The URL of the social media profile or website. Must be a valid HTTPS URL.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "personId",
        "platform",
        "url"
      ]
    },
    "GalleryPhoto": {
      "title": "GalleryPhoto",
      "type": "object",
      "description": "A photo in a person's gallery.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "treeId": { "type": "string" },
        "personId": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "storagePath": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "treeId", "personId", "url", "storagePath", "createdAt"]
    },
    "CanvasPosition": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CanvasPosition",
      "type": "object",
      "description": "Records the visual position of a person's card on the interactive family tree canvas, along with UI state like grouping and locking.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CanvasPosition entity."
        },
        "treeId": {
          "type": "string",
          "description": "Reference to the FamilyTree this canvas position belongs to. (Relationship: FamilyTree 1:N CanvasPosition)"
        },
        "personId": {
          "type": "string",
          "description": "Reference to the Person whose position is being stored. (Relationship: Person 1:N CanvasPosition)"
        },
        "x": {
          "type": "number",
          "description": "The X-coordinate (horizontal position in pixels) of the person's card on the canvas."
        },
        "y": {
          "type": "number",
          "description": "The Y-coordinate (vertical position in pixels) of the person's card on the canvas."
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when this canvas position was last updated.",
          "format": "date-time"
        },
        "isLocked": {
          "type": "boolean",
          "description": "If true, the node cannot be dragged or moved on the canvas."
        },
        "groupId": {
          "type": [
            "string",
            "null"
          ],
          "description": "An identifier for a logical group of nodes that should move together."
        }
      },
      "required": [
        "id",
        "treeId",
        "personId",
        "x",
        "y",
        "updatedAt"
      ]
    },
    "ManualEvent": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ManualEvent",
      "type": "object",
      "description": "A user-defined event on the calendar for a specific tree.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the event."
        },
        "userId": {
          "type": "string",
          "description": "Owner of the tree."
        },
        "treeId": {
          "type": "string",
          "description": "Parent family tree ID."
        },
        "title": {
          "type": "string",
          "description": "Title of the event."
        },
        "date": {
          "type": "string",
          "description": "Event date in YYYY-MM-DD format.",
          "format": "date"
        },
        "time": {
          "type": "string",
          "description": "Optional event time in HH:mm format."
        },
        "allDay": {
          "type": "boolean",
          "description": "True if the event is all-day."
        },
        "description": {
          "type": "string",
          "description": "Optional description for the event."
        },
        "color": {
          "type": "string",
          "description": "Hex color code for the event display."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "treeId",
        "title",
        "date",
        "allDay",
        "color",
        "createdAt",
        "updatedAt"
      ]
    },
    "ExportedFile": {
      "title": "ExportedFile",
      "type": "object",
      "description": "Metadata for a file exported by a user and saved to storage.",
      "properties": {
        "id": {
          "type": "string"
        },
        "userId": {
          "type": "string"
        },
        "treeId": {
          "type": "string"
        },
        "treeName": {
          "type": "string"
        },
        "fileName": {
          "type": "string"
        },
        "fileType": {
          "type": "string",
          "enum": [
            "pdf",
            "xlsx",
            "pptx",
            "png",
            "html",
            "docx"
          ]
        },
        "storagePath": {
          "type": "string"
        },
        "downloadURL": {
          "type": "string",
          "format": "uri"
        },
        "fileSizeBytes": {
          "type": "number"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "treeId",
        "treeName",
        "fileName",
        "fileType",
        "storagePath",
        "downloadURL",
        "fileSizeBytes",
        "createdAt"
      ]
    },
    "SharedTree": {
      "title": "SharedTree",
      "type": "object",
      "description": "Represents a tree shared from one user to another.",
      "properties": {
        "id": {
          "type": "string"
        },
        "ownerUserId": {
          "type": "string"
        },
        "ownerName": {
          "type": "string"
        },
        "treeId": {
          "type": "string"
        },
        "treeName": {
          "type": "string"
        },
        "sharedWithEmail": {
          "type": "string",
          "format": "email"
        },
        "sharedWithUserId": {
          "type": [
            "string",
            "null"
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "canEdit": {
          "type": "boolean"
        }
      },
      "required": [
        "ownerUserId",
        "ownerName",
        "treeId",
        "treeName",
        "sharedWithEmail",
        "createdAt",
        "canEdit"
      ]
    },
    "PublicTree": {
      "title": "PublicTree",
      "type": "object",
      "description": "A denormalized, public-facing representation of a family tree.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the original tree."
        },
        "ownerUserId": {
          "type": "string"
        },
        "ownerName": {
          "type": "string"
        },
        "treeName": {
          "type": "string"
        },
        "personCount": {
          "type": "number"
        },
        "relationshipCount": {
          "type": "number"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "coverPhotoURL": {
          "type": "string",
          "description": "A URL to the tree's cover photo. Must be a valid HTTPS URL.",
          "format": "uri"
        }
      },
      "required": [
        "ownerUserId",
        "ownerName",
        "treeName",
        "personCount",
        "relationshipCount",
        "createdAt"
      ]
    },
    "RootsProject": {
      "title": "RootsProject",
      "type": "object",
      "description": "A 'Shoreshim' (Family Roots) project for Israeli middle school students.",
      "properties": {
        "id": {
          "type": "string"
        },
        "userId": {
          "type": "string"
        },
        "treeId": {
          "type": "string"
        },
        "projectName": {
          "type": "string"
        },
        "currentStep": {
          "type": "number"
        },
        "projectData": {
          "type": "object"
        },
        "chatHistory": {
          "type": "array"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "treeId",
        "projectName",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile data. Document ID matches Firebase Auth UID ({userId}). This is a top-level collection for user-specific data."
        }
      },
      {
        "path": "/userEmails/{email}",
        "definition": {
          "entityName": "UserEmail",
          "schema": {
            "$ref": "#/backend/entities/UserEmail"
          },
          "description": "Maps user emails to UIDs to allow sharing lookups without exposing all user data."
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}",
        "definition": {
          "entityName": "FamilyTree",
          "schema": {
            "$ref": "#/backend/entities/FamilyTree"
          },
          "description": "Stores individual family tree projects owned by a user. Nested under the owning user's document. The 'userId' field in the document MUST match the '{userId}' path parameter and 'request.auth.uid'.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns this family tree, matching Firebase Auth UID."
            },
            {
              "name": "treeId",
              "description": "The unique identifier for the family tree document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/people/{personId}",
        "definition": {
          "entityName": "Person",
          "schema": {
            "$ref": "#/backend/entities/Person"
          },
          "description": "Stores individual person records within a specific family tree. Nested under the respective FamilyTree. Includes denormalized 'userId' and 'treeId' fields for authorization independence. These fields MUST match the corresponding path parameters.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent family tree, matching Firebase Auth UID."
            },
            {
              "name": "treeId",
              "description": "The unique identifier for the parent family tree."
            },
            {
              "name": "personId",
              "description": "The unique identifier for the person document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/people/{personId}/gallery/{photoId}",
        "definition": {
          "entityName": "GalleryPhoto",
          "schema": {
            "$ref": "#/backend/entities/GalleryPhoto"
          },
          "description": "Stores gallery photos for a person."
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/relationships/{relationshipId}",
        "definition": {
          "entityName": "Relationship",
          "schema": {
            "$ref": "#/backend/entities/Relationship"
          },
          "description": "Defines connections between people within a family tree. Nested under the respective FamilyTree. Includes denormalized 'userId' and 'treeId' fields for authorization independence. These fields MUST match the corresponding path parameters.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent family tree, matching Firebase Auth UID."
            },
            {
              "name": "treeId",
              "description": "The unique identifier for the parent family tree."
            },
            {
              "name": "relationshipId",
              "description": "The unique identifier for the relationship document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/canvasPositions/{positionId}",
        "definition": {
          "entityName": "CanvasPosition",
          "schema": {
            "$ref": "#/backend/entities/CanvasPosition"
          },
          "description": "Records the visual position of a person's card on the interactive family tree canvas. Nested under the respective FamilyTree. Includes denormalized 'userId' and 'treeId' fields for authorization independence. These fields MUST match the corresponding path parameters.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent family tree, matching Firebase Auth UID."
            },
            {
              "name": "treeId",
              "description": "The unique identifier for the parent family tree."
            },
            {
              "name": "positionId",
              "description": "The unique identifier for the canvas position document. Typically corresponds to a personId."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/people/{personId}/socialLinks/{socialLinkId}",
        "definition": {
          "entityName": "SocialLink",
          "schema": {
            "$ref": "#/backend/entities/SocialLink"
          },
          "description": "Stores external social media or website links associated with a specific person. Nested under the respective Person. Includes denormalized 'userId', 'treeId', and 'personId' fields for authorization independence. These fields MUST match the corresponding path parameters.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the parent family tree, matching Firebase Auth UID."
            },
            {
              "name": "treeId",
              "description": "The unique identifier for the parent family tree."
            },
            {
              "name": "personId",
              "description": "The unique identifier for the parent person."
            },
            {
              "name": "socialLinkId",
              "description": "The unique identifier for the social link document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/manualEvents/{eventId}",
        "definition": {
          "entityName": "ManualEvent",
          "schema": {
            "$ref": "#/backend/entities/ManualEvent"
          },
          "description": "Stores user-defined calendar events for a specific family tree. Path parameters must match denormalized fields.",
          "params": [
            {
              "name": "userId",
              "description": "Owner's UID."
            },
            {
              "name": "treeId",
              "description": "Parent tree's ID."
            },
            {
              "name": "eventId",
              "description": "The event's unique ID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/familyTrees/{treeId}/rootsProjects/{projectId}",
        "definition": {
          "entityName": "RootsProject",
          "schema": {
            "$ref": "#/backend/entities/RootsProject"
          },
          "description": "Stores the data for a user's Shoreshim project associated with a specific tree."
        }
      },
      {
        "path": "/exportedFiles/{fileId}",
        "definition": {
          "entityName": "ExportedFile",
          "schema": {
            "$ref": "#/backend/entities/ExportedFile"
          },
          "description": "Stores metadata for user-exported files. Document ID is auto-generated. Access is restricted to the owner."
        }
      },
      {
        "path": "/sharedTrees/{shareId}",
        "definition": {
          "entityName": "SharedTree",
          "schema": {
            "$ref": "#/backend/entities/SharedTree"
          },
          "description": "Manages access for trees shared between users."
        }
      },
      {
        "path": "/publicTrees/{treeId}",
        "definition": {
          "entityName": "PublicTree",
          "schema": {
            "$ref": "#/backend/entities/PublicTree"
          },
          "description": "Denormalized collection for trees marked as public, allowing for public browsing."
        }
      }
    ],
    "reasoning": "The proposed Firestore structure prioritizes security, scalability, and debuggability by strictly adhering to the core design principles, especially Authorization Independence. The primary change involves restructuring the data to a hierarchical path model under the authenticated user's ID, which significantly simplifies security rules and eliminates cross-document `get()` calls.\n\n**Key Justifications & Design Principles Applied:**\n\n1.  **Authorization Independence (CRITICAL):** The most significant improvement is the elimination of `get()` calls in security rules. In the original design, `people`, `relationships`, `canvasPositions`, and `socialLinks` all relied on fetching the parent `familyTree` document to determine the `userId` for authorization. This is a critical violation of Authorization Independence, making rules fragile and hard to debug, especially during atomic operations like batched writes or transactions.\n    \n    **Solution:**\n    *   **Hierarchical Paths:** All user-specific data is now nested directly under `/users/{userId}`. For instance, `familyTrees` are now at `/users/{userId}/familyTrees/{treeId}`, `people` at `/users/{userId}/familyTrees/{treeId}/people/{personId}`, and so on. This immediately makes `request.auth.uid` directly comparable to the `{userId}` wildcard in the path for most data, providing the highest level of implicit authorization.\n    *   **Denormalization:** For all subcollections of `familyTrees` (i.e., `people`, `relationships`, `canvasPositions`, and `socialLinks`), the `userId` (owner of the tree) and `treeId` are explicitly denormalized and stored as fields within each document. This means that a document like `Person` will now contain `userId` and `treeId` fields in addition to its own properties. Security rules can then simply check `request.auth.uid == userId` (from the path) AND `resource.data.userId == userId` (from the document) AND `resource.data.treeId == treeId` (from the document matching the path's `treeId`). This ensures that all necessary authorization context is present *within the document itself*, making authorization atomic and independent.\n\n2.  **Clarity of Intent (Debuggability):** The hierarchical path structure `/users/{userId}/familyTrees/{treeId}/...` makes the ownership explicit and immediately understandable. A glance at a document's path reveals its owner and the tree it belongs to. Security rules become much simpler, focusing on validating path parameters against `request.auth.uid` and ensuring denormalized fields match the path, making them easier to write, test, and debug.\n\n3.  **DBAC (No Custom Claims):** Roles are not required for this application, as all data is private and solely owned by the creating user. Authorization relies solely on `request.auth.uid` matching the `userId` in the path and the denormalized `userId` field, fully adhering to DBAC principles.\n\n4.  **QAPs (Rules are not Filters):** This structure inherently supports Query-As-Permissions (QAPs). By denormalizing `userId` and `treeId` into every sub-document, `list` queries can include `where('userId', '==', request.auth.uid)` and `where('treeId', '==', '{treeId}')` clauses. Because the rules enforce that these fields *must* match the path and `request.auth.uid`, any query that includes these filters will only return data the user is authorized to see, without the rules themselves needing to filter results.\n    *   For example, to list all people in a specific tree: `db.collection('users/{userId}/familyTrees/{treeId}/people').where('userId', '==', userIdFromAuth).where('treeId', '==', treeIdParam)`. The rules will guarantee that `userIdFromAuth` matches the `userId` in the path and document, and `treeIdParam` matches the `treeId` in the path and document.\n\n5.  **Invariants:** The new structure, combined with validation in security rules, strongly supports invariants like ownership and data integrity. For instance, when creating a new `Person` document under `/users/{userId}/familyTrees/{treeId}/people/{personId}`, the rules will enforce that `request.auth.uid` matches `{userId}` in the path, and that the `Person` document itself contains `userId: '{userId}'` and `treeId: '{treeId}'`.\n\n**Regarding the 'auth/invalid-api-key' error:** This error is not related to the data structure or security rules. It indicates a problem with your Firebase project configuration, specifically the API key provided in `src/lib/firebase.ts`. Please ensure that your `firebaseConfig` object contains the correct and complete credentials for your `family-tree-e7e90` project, as copied directly from your Firebase project settings (Project settings -> General -> Your apps -> Firebase SDK snippet -> Config). This typically happens if the `apiKey` field is missing, incorrect, or if the project ID is wrong."
  }
}
